# The Graphest Electron App

- [`common`] - files that are referenced by both the main and the renderer processes.
- [`main`] - the main process files.
- [`renderer`] - the renderer process files.
- [`typings`] - type declaration files.
